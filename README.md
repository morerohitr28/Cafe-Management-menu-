# Cafe-Management-menu-
A simple Python restaurant ordering system that displays a menu, takes up to two item orders, validates user input, and calculates the total bill. It includes a special rule preventing customers from ordering only coffee. Built using dictionaries, conditionals, and basic user input handling.
